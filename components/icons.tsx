import type { Icon as LucideIcon } from "lucide-react";
import { Loader2, X, CornerDownLeft, Menu } from "lucide-react";

export type Icon = LucideIcon;

export const Icons = {
  spinner: Loader2,
  close: X,
  back: CornerDownLeft,
  bars: Menu,
};
